{
  "name": "assdk500",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "latest",
    "@aws-sdk/client-rekognitionstreaming": "3.600.0",
    "@aws-sdk/middleware-websocket": "latest"
  }
}
